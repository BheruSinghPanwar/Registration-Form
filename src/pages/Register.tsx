import RegistrationForm from "../components/RegistrationForm";
// import "../styles/register.css"; 

const Register = () => {
  return (
    <div className="register-page">in register page
      <RegistrationForm/>
    </div>
  );
};

export default Register;
